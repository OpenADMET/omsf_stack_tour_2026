name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  schedule:
    # Run at 05:30 UTC every Monday
    - cron: '30 5 * * 1'
  workflow_dispatch:

jobs:
  test:
    name: Test on ${{ matrix.os }}, Python ${{ matrix.python-version }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        python-version: ["3.12"]

    steps:
      - uses: actions/checkout@v4

      - name: Additional info about the build
        shell: bash
        run: |
          uname -a
          df -h
          ulimit -a

      - uses: mamba-org/setup-micromamba@v1
        with:
          environment-file: environment.yml
          environment-name: oadmt_2026_demo
          condarc: |
            channels:
              - conda-forge
          create-args: >-
            python=${{ matrix.python-version }}
            click
            intake
            intake-parquet
            joblib
            loguru
            matplotlib
            numpy
            pandas
            rich
            seaborn
            statsmodels
            tabulate

      - name: Install openadmet and pytest-nbmake
        shell: bash -l {0}
        run: |
          python -m pip install git+https://github.com/OpenADMET/openadmet-models.git@main
          python -m pip install nbmake pytest

      - name: Verify openadmet CLI
        shell: bash -l {0}
        run: |
          which openadmet
          openadmet --help

      - name: Run CPU demo notebook
        shell: bash -l {0}
        run: |
          pytest --nbmake --nbmake-timeout=1200 --disable-warnings \
            01_train_and_finetune_CPU_demo/01_train_and_finetune.ipynb
